This program generates a P × P 2D array with random even numbers from 2 to 16. It displays the array and asks the user to enter an even number. Next need to highlight all occurrences of that number and displays how many times it appears.

Imported Random to generate random even numbers. Using 2D array, store the generated numbers. Then, use the needed conditional and looping statement to display the desired output
